{
  "name": "portfolioos-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "PortfolioOS Root Monorepo Architecture",
  "scripts": {
    "start": "cd backend && npm start",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "resend": "^6.24.0"
  }
}
